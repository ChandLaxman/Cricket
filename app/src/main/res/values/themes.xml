<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_active</item>
        <item name="colorPrimaryVariant">@color/blue_active</item>
        <item name="colorOnPrimary">@color/blue_active</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_active</item>
        <item name="colorSecondaryVariant">@color/blue_active</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>